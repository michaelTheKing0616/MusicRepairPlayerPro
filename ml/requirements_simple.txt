# ML Dependencies - Simplified (Without DeepFilterNet for easier installation)

# Core ML libraries
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Demucs - Source separation (can install without Rust)
demucs>=4.0.0

# Loudness normalization
pyloudnorm>=0.1.1

# Storage (if needed for direct Supabase access from Python)
supabase>=2.0.0

# Utilities
tqdm>=4.65.0
python-dotenv>=1.0.0

# Note: DeepFilterNet requires Rust/Cargo
# Install separately: pip install deepfilternet (after installing Rust)
# Or use fallback implementation (already in code)

