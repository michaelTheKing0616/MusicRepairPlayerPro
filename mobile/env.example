# Copy values into mobile/src/config/apiConfig.ts for local testing.
#
# Physical Android device on same Wi‑Fi as your PC:
#   DEV_API_HOST_OVERRIDE=192.168.1.42
#
# Backend must listen on 0.0.0.0:8000 (see backend/docker-compose.yml).
